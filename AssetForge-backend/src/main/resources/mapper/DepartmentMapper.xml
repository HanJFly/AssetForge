<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hjf.mapper.DepartmentMapper">

    <select id="getTree" resultType="com.hjf.vo.DepartmentVO">
            select * from department
            left join user on department.manager_user_id = user.id
            where department.is_deleted = 0
            order by department.sort_order

    </select>

    <select id="querypage" resultType="com.hjf.vo.DepartmentVO">
        select * from department
        left join user on department.manager_user_id = user.id
        where department.is_deleted = 0
        <if test="name != null and name != ''">
            and department.name like concat('%',#{name},'%')
        </if>

        order by department.sort_order

    </select>
</mapper>
